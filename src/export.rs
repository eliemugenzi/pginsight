//! Reserved for future non-interactive output formatters (table / json / csv).
//!
//! Not wired into `main.rs` yet — add `mod export;` and CLI subcommands when
//! building this out.

#![allow(dead_code)]
